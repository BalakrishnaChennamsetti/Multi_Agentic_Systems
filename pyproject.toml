[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "multi-agentic-systems"
version = "0.1.0"
dependencies = [
    "langgraph",
    "langchain-ollama",
    "ollama"
]

[tool.setuptools.packages.find]
where = ["src"]